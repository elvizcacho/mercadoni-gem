require 'spec_helper'

describe Mercadoni do
  it 'has a version number' do
    expect(Mercadoni::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
